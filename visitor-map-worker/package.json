{
  "name": "visitor-map-worker",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute visitor-map --remote --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
